{
  "name": "text-visualizer",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:analyze": "ANALYZE=true npm run build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:unit": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --reporter=html",
    "lint": "eslint src --ext .js,.ts,.jsx,.tsx --fix",
    "type-check": "tsc --noEmit",
    "security-scan": "node scripts/security-scan.js",
    "cost-check": "node scripts/cost-monitor.js",
    "rollback": "node scripts/rollback.js",
    "deploy": "node scripts/deploy.js deploy",
    "deploy:staging": "NODE_ENV=staging npm run deploy",
    "deploy:production": "NODE_ENV=production npm run deploy",
    "docker:build": "node scripts/deploy.js docker && docker build -t text-visualizer .",
    "docker:run": "docker run -p 3000:3000 text-visualizer"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@codemirror/basic-setup": "^0.20.0",
    "@codemirror/lang-html": "^6.4.9",
    "@codemirror/lang-xml": "^6.1.0",
    "@codemirror/state": "^6.5.2",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@codemirror/view": "^6.38.1",
    "axios": "^1.11.0",
    "codemirror": "^6.0.2",
    "vite": "^7.0.6"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^5.14.0",
    "terser": "^5.43.1",
    "vite-plugin-compression": "^0.5.1"
  }
}
